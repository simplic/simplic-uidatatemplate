<Window x:Class="Sample.UIDataTemplate.SampleEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sample.UIDataTemplate"
        mc:Ignorable="d"
        Title="SampleEditor" Height="450" Width="720">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBox FontFamily="Consolas" x:Name="codeTextBox" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" AcceptsReturn="True" />
        <Button Grid.Row="1" Width="100" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="3" Content="Save" Click="OnSaveButtonClick" />
    </Grid>
</Window>
